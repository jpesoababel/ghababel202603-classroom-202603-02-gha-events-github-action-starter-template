name: 02 - Eventos

on:
    push:
    pull_request:
    workflow_dispatch:
jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: ShowTrigger
              run: | 
                echo "Nombre Evento : ${{ github.event_name}}"

